## "Fork" repositori GitHub

git remote # untuk melihat remote yang ada
git remote -v # untuk melihat remote yang ada dengan URL
git remote set-url <nama-remote> <url-baru> # untuk mengubah URL remote
git push
